import { useState, useEffect, useRef, useContext } from 'react';
import api from '../../../../../config/api';

export default function Seguindo() {
  return (
    <div className='containerSeguindo' ref={modalRef}>
    </div>
  );
}
